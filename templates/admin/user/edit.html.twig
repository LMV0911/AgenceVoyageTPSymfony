{% extends 'base.html.twig' %}

{% block title %}Éditer
	{{ user.prenom }}
	{{ user.nom }}
{% endblock %}

{% block body %}
	<div class="container mx-auto px-4 py-8">
		<h1 class="text-2xl font-bold mb-6">Éditer
			{{ user.prenom }}
			{{ user.nom }}</h1>

		{{ form_start(form) }}
		{{ form_widget(form) }}
		<button class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700">
			Enregistrer les modifications
		</button>
		{{ form_end(form) }}
	</div>
{% endblock %}
