{% extends 'base.html.twig' %}

{% block title %}Modifier l'activité
{% endblock %}

{% block body %}
	<h1 class="text-2xl font-bold mb-6">Modifier l'activité</h1>

	<div class="mb-4">
		{{ include('activite/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
	</div>

	<a href="{{ path('app_activite_index') }}" class="text-blue-600 hover:underline">Retour à la liste</a>

	<div class="mt-6">
		{{ include('activite/_delete_form.html.twig') }}
	</div>
{% endblock %}
